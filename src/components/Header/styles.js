import styled from 'styled-components/native';
import styleApp from '../../config/style';

export const Container = styled.View`
  margin: 0 auto;
`;

export const Title = styled.Text`
  color: ${styleApp.primary.main};
  font-size: 25;
  font-weight: bold;
`;
